/**
 * @param {number[][]} matrix
 * @return {number}
 */
var maximalSquare = function (matrix) {
    if (matrix.length > 0) {

    } else {
        return 0;
    }
};

exports.maximalSquare = maximalSquare;
